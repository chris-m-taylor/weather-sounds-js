let data; // this will serve to capture the data we need from get requests.

function setup() {
    createCanvas(600,600)
    background(0)
}

function draw() {
    // draw something with them here.
}
